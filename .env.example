# 核心:提纯 / 嵌入 / 语义召回用的 DashScope(阿里云百炼)密钥。
# 不填也能用:浏览 + 关键词检索照常;提纯 / 嵌入 / 语义召回会降级或提示缺密钥。
ALIBABA_KEY=
ALIBABA_ENDPOINT=https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation
# 可选:仅 scripts/probe_models.py 做模型对比时才用;日常使用不需要。
MINIMAX_KEY=
MINIMAX_ENDPOINT=https://api.minimax.io/v1/text/chatcompletion_v2
# 可选:给记忆提纯 prompt 署名的角色名(如你的品牌 / 昵称);留空则用中性「记忆提纯器」。
PERSONA_NAME=
